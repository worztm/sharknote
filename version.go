package main

// AppVersion is the running Sharknote version. Bump it together with
// build/windows/info.json and build/windows/nsis/project.nsi on every
// release — the update checker compares this against the manifest
// published on the website (latest.json).
const AppVersion = "1.5.2"
